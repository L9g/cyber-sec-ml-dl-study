"""Ithuriel 探针信任核。

操作员候选探针编译出的纯逻辑（载荷/环境构造、分层 oracle、C2 统计判据）从
`scripts/run_calendar_probe.py` 搬入此处（P1，见 reports/partner-review-2026-07-22-codex.md）。
provider/AgentDojo glue 与 CLI 编排仍留 `scripts/`。
"""
